#lang plai

(define times (read))
(define made 0)

(define (run-til-times made times)
  (if (< made times)
      (begin
        (define tuples (read))
        (define elems (read-n-lines tuples '() 0))
        (my-map determine-function elems)
        (run-til-times (add1 made) times))))

(define (read-n-lines tuples lst times-read)
  (if (= tuples times-read)
      lst
      (begin
        (define x (read))
        (define y (read))
        (append lst (list x y))
        (read-n-lines tuples lst (add1 times-read)))))

(define (determine-function hash x y)
  (if (hash-has-key? hash x)
      (print 'NO)))